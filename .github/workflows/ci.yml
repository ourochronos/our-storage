name: CI

on:
  pull_request:
  push:
    branches: [main]
    paths-ignore:
      - '*.md'
      - 'docs/**'

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    uses: orobobos/oro-infra/.github/workflows/lint.yml@main
  test:
    needs: lint
    uses: orobobos/oro-infra/.github/workflows/test.yml@main
